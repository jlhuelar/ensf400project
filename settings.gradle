pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        // remove jcenter() unless you really need it, since it is deprecated
        maven { url 'https://plugins.gradle.org/m2/' }
    }
}

rootProject.name = 'ENSF400-FinalProject' 
