services:
  gradle-app:
    build:
      context: .
      dockerfile: burtonjong/ensf400fp:latest
    ports:
      - "8080:8080"
    container_name: gradle-app
    working_dir: /app
    command: ["./gradlew", "appRun", "--no-daemon"]
